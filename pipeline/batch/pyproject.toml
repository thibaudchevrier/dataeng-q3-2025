[project]
name = "batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "library",
]

[tool.uv.sources]
library = { path = "../library" , editable = true }

